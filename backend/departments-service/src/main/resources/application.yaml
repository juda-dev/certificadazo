server:
  port: ${PORT:0}

spring:
  application:
    name: departments-service

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3301/departments-db?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:judadev}
  jpa:
    hibernate:
      ddl-auto: update


eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
